var chai = require('chai');
var expect = chai.expect;
var xxx = require("../components/allMovies/allMoviesContainer.js");

describe('xxx', function() {
   it('should work', function() {
       expect(true).to.be.true;
   });

   it('should work', function() {
       expect(true).to.be.true;
   });

});